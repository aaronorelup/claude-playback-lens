{
  "name": "claude-playback-lens-mcp",
  "version": "0.3.2",
  "description": "MCP server over Claude Playback Lens — searches the timeline of Claude Code's own recorded transcripts (prompts, tool calls, file operations) and reports token/cost usage and session structure. The npm package ships the engine and the MCP server; the dev-mode web UI ships with the git repo.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "claude-playback-lens-mcp": "./mcp/lens-mcp.mjs"
  },
  "files": [
    "lens.mjs",
    "server/",
    "shared/",
    "mcp/"
  ],
  "scripts": {
    "start": "node lens.mjs",
    "mcp": "node mcp/lens-mcp.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "release": "node scripts/release.mjs",
    "prepublishOnly": "node scripts/check-release.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
